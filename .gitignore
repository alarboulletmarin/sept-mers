node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
coverage
# Les captures pleine page du parcours, pour déboguer. Celles du README, elles,
# sont versionnées : elles vivent dans `docs/captures/` et se regénèrent avec
# `node scripts/smoke.mjs --captures`.
shots

# Artefacts de compilation incrémentale de TypeScript.
*.tsbuildinfo
